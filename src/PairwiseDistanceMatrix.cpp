#include <iostream>
using namespace std;

double PairwiseDistanceMatrix(double x[], double y[]){
    int i, j, n;
  //  n = x.size();

  //  double pwdmat[][] = { };

    for(i = 0; i < n; i++){
        for(j = 0; j < n; j++){
       //    pwdmat[i][j] = pow( pow( x[i] - x[j], 2) + pow( y[i] - y[j], 2), 0.5);
    }
  }

 // return pwdmat;
}
